

#include <stdio.h>

int main()
{
  int num,arr[100],i;
  float q=0,p=0,r=0;
  
  scanf("%d",&num);
  for( i=0;i<num;i++)
  {
  scanf("%d",&arr[i]);
  
  
  if(arr[i]>0)
  
      p++;
  
  else if(arr[i]<0)
  
      q++;
  
   else
      r++;
  }
  
  printf("%f\n %f\n %f",p/num,q/num,r/num);
}
