

#include <stdio.h>
int main ()
{
    int t,i;
    long long  n,m,s,result;
    scanf("%d",&t);
    
    for(i=0;i<t;i++)
    {
        scanf("%lld %lld %lld",&n,&m,&s);
        result = ((m+s-1)%n);
        if(result ==0)
        result = n;
        printf("%lld\n",result);
    }
}
