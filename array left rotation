

#include <stdio.h>
int main()
{
    int rotation,i,size;
    scanf("%d%d",&size,&rotation);
   int arr[size],s[size];
   for(i=0;i<size;i++)
   {
       scanf("%d",&arr[i]);
   }
   for(i=0;i<size;i++)
   {
       s[i]= arr[(rotation+i)%size];
   }
   for(i=0;i<size;i++)
   printf("%d ",s[i]);
}
