


#include <stdio.h>

int main(){
    int n,i,max=0;
    scanf("%d",&n);
    
int arr[n];
int count =0;
for(i=1;i<=n;i++)
{
scanf("%d",&arr[i]);
  
    if(arr[i]>max)
    max = arr[i];
    
     
}
for(i=1;i<=n;i++){
    if(arr[i]== max)
    count++;
}
printf("%d",count++);
    
}
